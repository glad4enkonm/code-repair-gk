"""Vendored subset of the `tool` package.

Only the modules required by the knowledge-graph pipeline are included:
graph query operations, JSON function-call extraction, and the system
prompt builder. See tool/README.md for what was trimmed relative to
upstream and why.
"""

__version__ = "0.1.0-vendored"
