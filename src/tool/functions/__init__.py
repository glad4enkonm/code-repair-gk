"""Vendored subset of tool.functions.

Only graph_file_ops is included; the upstream package additionally
exports file operations, execute_command, ask_user, figma, git and
web-ui helpers, which the knowledge-graph pipeline does not use.
"""
